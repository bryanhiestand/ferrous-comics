Create the next GH release. Show me the release text and details before doing it

---

Wait until all CI has completed for this release, then determine how much faster we made the builds with our optimizations in this release. Then update the release notes with those details

---

we should compare 1.3.0 GHA times to 1.4.0 GHA times

---

is there a newer version of softprops/action-gh-release available that we can update node js version in? Is it still maintained? If not, should we use something else?